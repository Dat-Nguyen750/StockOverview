services:
  - type: web
    name: stock-evaluator-web
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    envVars:
      - key: PORT
        value: 8000
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production 